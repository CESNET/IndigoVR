- name:  Sign certificate
  shell: openssl ca -config "{{ ca_dir }}/openssl.cnf" -extensions usr_cert -days 365 -notext -batch -md sha512 -in "{{ ca_dir }}/certs/{{ cert_name }}.csr" -out "{{ ca_dir }}/certs/{{ cert_name }}.crt"
  args:
    creates: "{{ ca_dir }}/certs/{{ cert_name }}.crt"

